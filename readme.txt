This is the backend server for lung sound classification to detect respiratory diseases.

Two pre trained models are included in models folder.

To run the fask server, use the command python services.py